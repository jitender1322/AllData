#include <stdio.h>
#include <string.h>

// union student
// {
//    int id;
//    char name[100];
//    int age;
// } s1, s2;

enum
{
   one=5,
   two,
   three,
   four
};

int
main()
{

   printf("%d",four);
   // struct student s1;
   // struct student s2;

   // s1.id = 1;
   // strcpy(s1.name, "Ramesh");
   // s1.age = 23;

   // s2.id = 2;
   // strcpy(s2.name, "Suresh");
   // s2.age = 27;

   // printf("%u", sizeof(s1));

   // printf("%d ", s1.id);
   // printf("%s ", s1.name);
   // printf("%d \n\n\n", s1.age);

   // printf("%d %s %d ", s2.id, s2.name, s2.age);
   // printf("%s ",s2.name);
   // printf("%d ",s2.age);
}
